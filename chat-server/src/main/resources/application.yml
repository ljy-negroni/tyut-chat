spring:
  profiles:
    active: dev
  application:
    name: chat-server

websocket:
  enable: true
  port: 8878

tcpsocket:
  enable: false # 暂时不开启
  port: 8879

jwt:
  accessToken:
    secret: MIIBIjANBgkq  # 跟 chat-platform 的 secret 必须一致
