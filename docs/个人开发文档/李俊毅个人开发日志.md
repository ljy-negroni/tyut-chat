# 李俊毅 个人开发日志

> 项目：TYUT CHAT（太原理工大学校园即时通讯平台）
> 角色：全栈开发工程师
> 开发周期：2026.06.15 — 2026.06.25
> 开发模式：单人全栈，从零构建

---

## 2026.06.15（需求分析与架构设计）

### 当日工作计划
- 完成项目需求分析文档编写
- 完成系统架构设计与技术选型
- 确定模块划分与依赖关系
- 搭建基础开发环境

### 当日工作内容

**1. 需求分析（2 小时）**
- 分析课设要求，明确项目目标：构建面向太原理工大学师生的校园即时通讯 Web 应用
- 梳理核心功能需求：用户注册登录、私聊消息、群聊消息、图片/文件/语音发送、音视频通话（WebRTC）、好友管理、群组管理、消息撤回删除、敏感词过滤
- 撰写《需求分析文档》，包含功能需求矩阵、非功能需求（性能、安全、可扩展性）、数据流图

**2. 系统架构设计（2 小时）**
- 确定采用 B/S 架构 + 微服务思想，后端拆分为 4 个 Maven 模块：
  - `chat-common`：公共包，定义共享枚举、工具类、JWT 工具
  - `chat-client`：消息推送 SDK，封装 Redis 队列写入逻辑，供 platform 调用
  - `chat-server`：WebSocket 推送服务，基于 Netty，消费 Redis 队列向客户端推送消息
  - `chat-platform`：业务平台服务，基于 Spring Boot 3.3，提供 REST API
- 前端独立工程 `chat-web`，Vue 2 + Element UI + Pinia 状态管理
- 依赖方向严格单向：common ← client ← platform，common ← server

**3. 技术选型（1 小时）**
- 后端：Spring Boot 3.3、Netty 4.x（WebSocket 长连接）、MyBatis-Plus 3.5（ORM）、Druid（连接池）、Redis + Redisson（缓存/分布式锁/消息队列）、MinIO（对象存储）、JWT（无状态认证）、Knife4j（API 文档）
- 前端：Vue 2.7、Element UI 2.15、Pinia 2.1（状态管理）、Axios（HTTP 请求）、Three.js 0.160（3D 背景）、SCSS（样式预处理）
- 存储：MySQL 8.0（关系型数据）、Redis 6.2（缓存/消息队列）、MinIO（图片/文件/语音）
- 构建：Maven 多模块 + JDK 17

**4. 开发环境搭建（2 小时）**
- 安装配置 JDK 17，设置 JAVA_HOME 环境变量
- 安装 Maven 3.9，配置 settings.xml 镜像源为阿里云
- 安装 MySQL 8.0，创建数据库 `im_platform_open`，设置字符集 utf8mb4
- 安装 Docker Desktop（Windows 11 Home，WSL 2 引擎），拉取并启动 Redis 6.2 容器（端口 6379）
- 拉取并启动 MinIO 容器（端口 9000/9001），数据目录挂载到 E 盘
- 初始化 Git 仓库，配置 .gitignore 忽略 node_modules、target、*.iml 等

### 遇到的问题及解决方法
| 问题 | 原因 | 解决方法 |
|------|------|----------|
| Redis 启动后无法连接 | Docker 容器未绑定端口 | `-p 6379:6379` 映射端口 |
| MinIO Access Key 认证失败 | 配置文件中密钥与容器不一致 | 统一使用 admin/admin123456 |
| Windows Home 版不支持 Hyper-V | 系统版本限制 | 使用 WSL 2 引擎替代 |

### AI 使用记录
- Claude：协助梳理 Spring Boot 3.3 与 JDK 17 的兼容性注意事项，确认各依赖版本无冲突
- Claude：辅助检查 MySQL 建表 SQL 语法，验证索引设计合理性
- 所有技术选型决策由本人独立完成，AI 仅提供信息检索辅助

---

## 2026.06.16（数据库设计与后端基础搭建）

### 当日工作计划
- 完成数据库 ER 图设计与建表 SQL
- 搭建 Spring Boot 多模块项目骨架
- 完成用户注册登录模块
- 配置 JWT 认证拦截器

### 当日工作内容

**1. 数据库设计（2 小时）**
- 设计 8 张核心业务表：
  - `im_user`：用户表（id, user_name, nick_name, password, head_image, sex, signature, is_banned, reason, created_at）
  - `im_friend`：好友关系表（id, user_id, friend_id, friend_nick_name, friend_head_image, is_dnd, version）
  - `im_group`：群组表（id, group_name, avatar, owner_id, announcement, max_members, is_banned）
  - `im_group_member`：群成员表（id, group_id, user_id, user_nick_name, head_image, role, is_dnd, version）
  - `im_private_message`：私聊消息表（id, send_id, recv_id, type, content, status, send_time）
  - `im_group_message`：群聊消息表（id, group_id, send_id, type, content, status, send_time）
  - `im_sensitive_word`：敏感词表（id, content, enabled）
  - `im_message_deletion`：消息删除记录表
- 编写 DDL 脚本，使用 InnoDB 引擎，utf8mb4 字符集，合理建立索引（user_name 唯一索引、send_time 查询索引）
- 编写版本化 SQL 脚本存放于 `db/更新脚本/`

**2. Maven 多模块项目搭建（1.5 小时）**
- 创建根 pom.xml，声明 Spring Boot 3.3、MyBatis-Plus 3.5、Hutool 5.8.28、MinIO 8.5 等版本
- 创建 chat-common 模块：定义 IMTerminalType 枚举、JwtUtil 工具类、IMClient 接口
- 创建 chat-client 模块：实现 IMClient，封装 Redis List 的消息入队操作
- 创建 chat-server 模块：配置 Netty WebSocket 服务端（端口 8878），实现 ChannelHandler 处理连接/断开/消息转发
- 创建 chat-platform 模块：配置 Spring Boot Web 应用（端口 8088），集成 MyBatis-Plus、Druid、Redis、Knife4j

**3. 用户认证模块开发（3 小时）**
- 编写 User 实体类，使用 MyBatis-Plus 注解映射 im_user 表
- 编写 UserMapper 接口，继承 BaseMapper<User>
- 编写 UserService 接口与 UserServiceImpl 实现：
  - `register()`：校验用户名唯一性、敏感词过滤、BCrypt 密码加密、写入数据库
  - `login()`：查询用户、验证密码、生成 JWT accessToken + refreshToken
  - `refreshToken()`：验证 refreshToken 有效性，颁发新 token
  - `modifyPassword()`：旧密码验证后更新
- 编写 JwtProperties 配置类，读取 application.yml 中的 secret 与过期时间
- 编写 AuthInterceptor 拦截器，从请求头提取 accessToken，验证签名，存放 SessionContext
- 编写 MvcConfig，注册拦截器并配置白名单（/login, /register, /refreshToken, /captcha, Swagger 路径）
- 编写 LoginController，提供 POST /login、POST /register、PUT /refreshToken、PUT /modifyPwd

### 遇到的问题及解决方法
| 问题 | 原因 | 解决方法 |
|------|------|----------|
| MyBatis-Plus 启动报错 | 未排除旧版 mybatis-spring | 手动指定 3.0.5 版本 |
| JWT 解析失败 | secret 配置未注入 | 在 JwtProperties 添加 @ConfigurationProperties |
| Knife4j 文档页面 404 | Spring Boot 3.x 路径变更 | 使用 openapi3-jakarta 版本 |

### AI 使用记录
- Claude：辅助排查 MyBatis-Plus 与 Spring Boot 3.x 的自动配置冲突，定位到 mybatis-spring 版本不兼容问题
- Claude：提供 BCrypt 密码编码器配置示例，本人根据项目结构调整后集成
- GitHub Copilot（IDEA 插件）：辅助生成实体类 Getter/Setter 及简单 CRUD 样板代码

---

## 2026.06.17（消息系统与 WebSocket 推送）

### 当日工作计划
- 完成 WebSocket 推送服务（chat-server）
- 实现私聊消息发送与接收流程
- 实现消息已读/未读状态同步
- 实现 Redis 消息队列跨节点路由

### 当日工作内容

**1. Netty WebSocket 服务端（3 小时）**
- 编写 WebSocketServer 启动类，绑定端口 8878，配置 BossGroup/WorkerGroup 线程池
- 编写 IMChannelHandler，处理 channelActive（连接建立）、channelInactive（断开）、channelRead（消息读取）
- 编写 LoginProcessor，解析客户端登录帧，绑定 userId 与 Channel 映射关系，存入本地 ConcurrentHashMap
- 编写 PrivateMessageProcessor，处理私聊消息的转发逻辑：
  - 接收方在线 → 直接通过 WebSocket 推送
  - 接收方离线 → 不处理，等待对方上线后拉取历史

**2. 消息推送链路（2 小时）**
- 设计 Redis List 消息队列模型：每台 chat-server 实例持有独立的队列 key `im:unread:{serverId}`
- 用户登录时，在 Redis 中记录 `im:user_server:{userId}` → serverId 映射
- Platform 发送消息时：查询接收方所在 server → 将消息 JSON 写入对应 Redis List
- Server 消费线程：定时 BLPOP 自己的队列，取出消息后通过 WebSocket 推送给目标用户

**3. 私聊消息 API（2 小时）**
- 编写 PrivateMessageController，提供：
  - POST /message/private/send：发送私聊消息
  - GET /message/private/history：拉取历史消息（分页，按时间倒序）
  - DELETE /message/private/delete：删除单条消息
  - PUT /message/private/recall：撤回消息（2 分钟内）
- 编写 PrivateMessageService 实现，处理消息体序列化、敏感词过滤、状态流转（SENDING → DELIVERED → READED）

**4. 消息状态管理（1.5 小时）**
- 定义 MESSAGE_STATUS 枚举：FAILED(-2)、SENDING(-1)、PENDING(0)、DELIVERED(1)、RECALL(2)、READED(3)
- 实现已读回执：客户端收到消息后发送 ACK 帧，服务端更新消息状态为 DELIVERED
- 实现已读上报：用户进入会话时发送 READ 帧，服务端批量更新该会话所有消息为 READED

### 遇到的问题及解决方法
| 问题 | 原因 | 解决方法 |
|------|------|----------|
| WebSocket 连接频繁断开 | Netty 未配置心跳 | 添加 IdleStateHandler，60s 读写空闲发送 PING |
| 消息乱序问题 | 异步推送未排队 | 使用 LinkedBlockingQueue 顺序处理同一用户的推送 |
| 大消息体导致 OOM | 图片 base64 直接存消息体 | 图片改为上传 MinIO，消息体只存 URL |

### AI 使用记录
- Claude：辅助分析 Netty ChannelHandler 中消息乱序的根本原因（并发写入不同 Channel 的竞态条件），建议使用 LinkedBlockingQueue 串行化同一用户推送
- Claude：协助调试 WebSocket 频繁断开问题，定位到缺少 IdleStateHandler 心跳机制
- GitHub Copilot：辅助编写 GroupMessageProcessor 中的群成员遍历循环

---

## 2026.06.18（文件上传、群聊与 WebRTC）

### 当日工作计划
- 集成 MinIO 对象存储，实现文件/图片上传
- 实现群聊消息功能
- 搭建 WebRTC 音视频通话信令服务

### 当日工作内容

**1. MinIO 集成（2 小时）**
- 编写 MinioProperties 配置类：endpoint、accessKey、secretKey、bucketName、imagePath、filePath、videoPath
- 编写 MinioService 工具类：
  - `bucketExists()`：检查 bucket 是否存在
  - `makeBucket()`：创建 bucket
  - `setBucketPublic()`：设置 bucket 公开读策略（JSON Policy 格式）
  - `upload()`：上传文件，生成 UUID 文件名，返回访问 URL
  - `remove()`：删除文件
- 编写 FileController：POST /image/upload、POST /file/upload，返回缩略图与原图 URL
- 使用 Thumbnailator 生成图片缩略图（宽度 300px），减小前端加载压力

**2. 群聊功能（2.5 小时）**
- 编写 Group 实体类、GroupMember 实体类、GroupMessage 实体类
- 编写 GroupService 实现：创建群聊、邀请成员、踢出成员、修改群公告、解散群聊
- 编写 GroupMessageService 实现：群消息发送、@提醒、群消息历史拉取
- 编写 GroupMessageProcessor（chat-server 端）：群消息广播逻辑，遍历群成员列表逐个推送

**3. WebRTC 信令服务（2.5 小时）**
- 编写 WebrtcPrivateController，处理一对一通话信令：呼叫、接受、拒绝、挂断、ICE Candidate 交换
- 编写 WebrtcGroupController，处理多人通话：创建房间、加入房间、退出房间、Offer/Answer/Candidate 中继
- 信令消息通过 WebSocket 而非 HTTP 传输，减少延迟
- 配置 STUN 服务器（Google STUN），NAT 穿透

### 遇到的问题及解决方法
| 问题 | 原因 | 解决方法 |
|------|------|----------|
| MinIO 上传后图片无法访问 | bucket 默认私有 | 调用 setBucketPublic 设置为公开读 |
| 群消息广播性能差 | 循环逐个推送阻塞线程 | 改用线程池异步推送 |
| WebRTC 连接失败（同一局域网） | 缺少 TURN 服务器 | 先使用 Google STUN 验证，TODO：部署自建 TURN |

### AI 使用记录
- Claude：辅助排查 MinIO bucket 权限问题，分析 bucketExists/makeBucket/setBucketPublic 调用时序，定位 setBucketPublic 仅在首次创建时执行的逻辑缺陷
- Claude：提供 MinIO SetBucketPolicy 所需的 JSON Policy 格式模板
- GitHub Copilot：辅助编写 Thumbnailator 缩略图生成代码（固定宽度缩放 + 保持比例）

---

## 2026.06.22（验证码系统、安全加固、环境配置本地化）

### 当日工作计划
- 实现图形验证码功能，防止机器人注册
- 将数据库密码等敏感配置改为环境变量占位
- 解决 MinIO 生产环境配置问题

### 当日工作内容

**1. 图形验证码系统（3 小时）**
- 在 chat-platform pom.xml 中添加 Hutool 5.8.28 依赖（使用 CaptchaUtil 工具类）
- 编写 CaptchaController 控制器：
  - GET /captcha：调用 `CaptchaUtil.createLineCaptcha(120, 42, 4, 20)` 生成 4 位数字线段干扰验证码
  - 生成 UUID 作为 key，将验证码文本存入 Redis：`SET captcha:{uuid} code EX 300`（5 分钟过期）
  - 返回 JSON：`{captchaKey: uuid, captchaImage: base64Data}`（图片 Base64 编码，直接嵌入 img 标签）
- 修改 RegisterDTO：新增 `captchaKey`（@NotEmpty）、`captchaCode`（@NotEmpty）字段
- 修改 LoginDTO：新增 `captchaKey`、`captchaCode` 字段（非必填，兼容自动登录场景）
- 修改 UserServiceImpl.register()：从 Redis 读取验证码文本，忽略大小写比对，校验后删除 key 防止复用
- 修改 UserServiceImpl.login()：仅当 captchaKey 非空时校验验证码（注册后自动登录场景不过验证码）
- 修改 MvcConfig 拦截器白名单：添加 `/captcha` 路径，允许未登录时获取验证码
- 前端 Login.vue 与 Register.vue 各添加验证码输入框 + 图片展示区，图片点击可刷新

**2. 配置文件安全加固（1 小时）**
- 将 application-dev.yml 中的硬编码数据库连接信息改为环境变量占位：
  - `url: jdbc:mysql://${MYSQL_HOST:localhost}:${MYSQL_PORT:3306}/...`
  - `username: ${MYSQL_USER:root}`
  - `password: ${MYSQL_PASSWORD:root}`
- Redis 连接同理：`host: ${REDIS_HOST:localhost}`、`port: ${REDIS_PORT:6379}`
- MinIO 外网访问 domain 改为：`http://${MINIO_DOMAIN:localhost}:9000`

**3. MinIO 配置修复与测试（1.5 小时）**
- 发现 FileServiceImpl 初始化逻辑缺陷：`setBucketPublic()` 仅在 bucket 首次创建时调用，若 bucket 已存在则跳过，导致重启后 bucket 权限丢失
- 修改逻辑：将 `setBucketPublic()` 移到 `if (!bucketExists)` 外部，确保每次启动都调用
- 测试：上传图片 → 浏览器直接访问 URL → 确认 200 响应
- 发现 MinIO domain 写死为旧 IP 192.168.2.67，改为环境变量后当前 IP 为 192.168.31.52（WiFi DHCP 变化）

### 遇到的问题及解决方法
| 问题 | 原因 | 解决方法 |
|------|------|----------|
| 验证码图片不显示（前端） | AuthInterceptor 拦截了未登录的 /captcha 请求 | MvcConfig 添加 /captcha 白名单 |
| 验证码图片 URL 错误 ERR_INVALID_URL | Hutool getImageBase64Data() 已含 `data:image/png;base64,` 前缀，前端又拼接了一次 | 前端去掉前缀拼接，直接使用返回值 |
| 图片外网访问超时 | MinIO domain 指向旧 IP | 改为环境变量 ${MINIO_DOMAIN}，默认 localhost |
| 平台重启后图片 403 | setBucketPublic 只在创建时调用 | 移到 if 外，每次启动都执行 |

### AI 使用记录
- Claude：辅助设计验证码完整流程（生成 → Redis 存储 → 校验 → 销毁），提供 Hutool CaptchaUtil API 用法示例
- Claude：协助定位验证码图片加载失败的三个根因：AuthInterceptor 拦截未登录请求 → 添加白名单；Hutool getImageBase64Data() 已含 data URI 前缀 → 前端去掉重复拼接；前端 getCaptcha() 缺少错误处理 → 添加 .catch() 日志
- Claude：提供 RedisTemplate 与 StringRedisTemplate 选型建议（验证码场景只需字符串操作，使用 StringRedisTemplate 更简洁）
- 验证码参数（4 位数字、120×42 尺寸、5 分钟过期）由本人根据安全需求自行决定

---

## 2026.06.23

---

## 2026.06.23（前端 UI 全面升级 —— macOS Vibrancy 暗色主题）

### 当日工作计划
- 完成品牌视觉重塑（太理朋友圈 → TYUT CHAT）
- 登录/注册页整体重设计（暗色玻璃风格 + 3D 粒子背景）
- 聊天界面气泡样式统一
- 全局 UI 细节打磨（在线状态、未读徽标、搜索框、头像）

### 当日工作内容

**1. 品牌重塑（1 小时）**
- 设计并绘制全新 SVG Logo：纯黑圆角方底（#0a0a0c）+ 双层白色聊天气泡 + 白色渐变输入指示点。保留原有聊天气泡造型，去除蓝紫渐变，适配暗色主题
- 修改 public/index.html：`<title>` 改为「TYUT CHAT」，`<meta name="theme-color">` 改为 `#0a0a0c`，`<meta name="description">` 更新
- 修改 .env.development 与 .env.production 中的 VUE_APP_NAME 为「TYUT CHAT」
- 修改后端 SwaggerConfig：接口文档标题、描述、联系人全部改为 TYUT CHAT
- 全局代码搜索替换「太理朋友圈」→「TYUT CHAT」

**2. 登录/注册页 macOS Vibrancy 暗色重设计（5 小时）**

*布局重构：*
- 弃用原有居中单栏布局，改为左品牌 + 右表单分栏（CSS Flexbox，响应式 @media 768px 断点切换为上下排列）
- 左侧品牌区：衬线字体 "TYUT CHAT" 标题（Georgia / Noto Serif SC，clamp(36px, 5vw, 64px) 响应式字号），副标题"太原理工大学 · 校园社区"（白色 0.45 透明），描述文字"连接校园每一刻"（白色 0.28 透明）
- 右侧面板区：暗灰毛玻璃卡片 `rgba(28,28,30,0.72)` + `backdrop-filter: blur(60px)` + `-webkit-backdrop-filter: blur(60px)`，圆角 16px，1px 白色透明边框 `rgba(255,255,255,0.08)`

*Three.js 3D 粒子背景改造：*
- 场景背景从浅紫 #ebeef7 改为纯黑 #0a0a0c，雾效同步
- 环境光从紫调 #d8ddf2 改为中性灰白 #333344（克制亮度）
- 浮动几何体（12 个 Icosahedron/Octahedron/Tetrahedron/TorusKnot）：颜色从亮蓝紫 HSL（0.67, 0.45, 0.4+）改为极淡灰 HSL（0.6, 0.08, 0.22+），金属度 0.3，粗糙度 0.7
- 粒子环（4 层共 800+ 粒子）：颜色从亮紫改为灰白 HSL（0.6, 0.04, 0.35+），透明度 0.18，叠加混合
- 极光丝带（5 层 ShaderMaterial）：颜色从紫系 `['#5846E8','#7668F2','#4432D0','#6858EC','#3624C0']` 改为无彩色灰系 `['#3a3a44','#4a4a55','#353544','#454555','#3d3d48']`
- 光环（3 层 Torus）：颜色饱和度降至 0.05
- 移除：校徽 3D 纹理（emblemTex）、3D 文字覆盖（"太理朋友圈"/"创建账号"标题 + "加入太原理工大学校园社区"/"连接校园每一刻"副标题）、圆形校徽底衬、网格地板
- 保留：鼠标跟随相机微移（lerp 插值）、浮动体正弦轨迹动画、粒子自转、光环旋转
- 新增：vignette 暗角遮罩（径向渐变，增强左侧文字对比度）

*表单组件 macOS 风格化：*
- 输入框：深色半透明底 `rgba(28,28,30,0.6)`，1px `rgba(255,255,255,0.1)` 边框，10px 圆角，白色 0.88 透明度文字，placeholder 白色 0.25，focus 时边框变亮至 0.22
- 按钮：纯色 `#3a3a3c` 背景，无渐变无阴影，白色 0.9 文字，hover 微亮至 `#4a4a4c`，active 变暗至 `#2c2c2e`，transition 0.2s ease
- 验证码图片：48px 高度，10px 圆角，1px 白色透明边框，hover 边框变亮
- 复选框：暗底白色边框，选中态白色半透明填充
- 链接文字：白色 0.35 透明，hover 变亮

**3. 聊天界面风格统一（2.5 小时）**

*消息气泡：*
- 对方消息（.message-text）：浅灰白底 `#e9e9eb`（iMessage 经典色），黑字 `#1c1c1e`，圆角 `8px 18px 18px 18px`，去阴影
- 自己消息（.message-mine .message-text）：macOS 蓝底 `#0a84ff`，白字 `#FFFFFF`，圆角 `18px 8px 18px 18px`

*侧边栏搜索框：*
- Chat.vue、Friend.vue、Group.vue 三处搜索框统一风格
- 暗底 `rgba(255,255,255,0.06)`，1px `rgba(255,255,255,0.08)` 边框，8px 圆角，白色半透明文字
- 添加按钮（好友/群聊）：透明底 + 白色边框，hover 变亮

*在线状态指示器：*
- 替换亮绿色 `#00ff00` / `limegreen` 为 macOS 标准绿 `#30d158`
- 好友列表：移除移动设备在线图标（el-icon-mobile-phone），保留电脑在线图标（el-icon-monitor），颜色改为白色 0.45 透明
- 头像角标：绿点 12px → 保持尺寸，背景改 #30d158

*未读消息徽标：*
- 类名从 unread-text 重构为 unread-badge
- 样式：红底 `#FF3B30`，白字，18px 高度，9px 半圆圆角，flex 居中
- 逻辑：≤99 显示原数字，>99 显示 "99+"（新增 unreadLabel 计算属性）

*Friend.vue 分组字母标签：*
- 颜色从紫色 `rgba(55,72,199,0.35)` 改为白色 0.25 透明

**4. 默认头像系统改造（0.5 小时）**
- 弃用原有 6 色鲜艳调色板（绿、红、粉、紫、黄、蓝）
- 设计 8 色 macOS Vibrancy 暗色调色板：`#3a3a3c` / `#4a4a58` / `#3d3848` / `#3c4450` / `#484842` / `#463a3e` / `#3e4248` / `#45404c`
- 文字从黑字改为白字 `rgba(255,255,255,0.88)`
- 背景色根据用户名 hashCode 取模分配
- 去除非 .avatar-text 的 color: white 硬编码（改为内联样式）

### 遇到的问题及解决方法
| 问题 | 原因 | 解决方法 |
|------|------|----------|
| Three.js 场景颜色漂移 | 灯光 + fog + background 色温不一致 | 统一为 #0a0a0c + 中性灰白灯光 |
| Safari 毛玻璃不生效 | 缺少 -webkit-backdrop-filter 前缀 | 添加 webkit 前缀 |
| ChatItem 布局错位 | CSS display:flex→inline-block 替换扩散 | git checkout 恢复，精准替换仅 .unread-badge |
| 浮动粒子在亮底上看不清 | 原紫色粒子在新暗底上可见 | 改为灰白粒子，提高对比度 |
| el-icon-key 不存在 | Element UI 2.x 无此图标 | 改为 el-icon-picture-outline |

### AI 使用记录
- Claude：辅助完成 macOS Vibrancy 设计系统的 Token 字典转换（Tailwind class → 原生 SCSS），包括边框颜色 rgba(255,255,255,0.08)、层级背景色 #1c1c1e/#2c2c2e/#3a3a3c、圆角规范 8-16px
- Claude：协助将原有 Three.js 紫色调场景参数批量替换为暗色灰白调（灯光色温、粒子 HSL、极光 Shader uniform），本人逐项确认色彩倾向
- Claude：协助排查 Safari 毛玻璃不生效问题，定位到 -webkit-backdrop-filter 前缀缺失
- Claude：提供 iMessage 气泡圆角参数参考（对方 8px 18px 18px 18px，自己 18px 8px 18px 18px）
- Claude：协助定位 ChatItem 未读徽标 CSS 修改时的选择器扩散问题（display:flex → inline-block 误匹配了父级 .chat-item），使用 git diff 逐行审查后修复
- 整体设计方向（macOS 暗色风格、1px 边框哲学、无渐变无大阴影）由本人确定，AI 辅助具体实现

---

## 2026.06.24

---

## 2026.06.24（功能测试、Bug 修复、文档编写）

### 当日工作计划
- 对已完成功能进行系统性测试，覆盖核心业务场景
- 修复测试中发现的 UI 错位与兼容性问题
- 编写个人开发日志
- 编写前端 UI 改造计划书，归档课设文档

### 当日工作内容

**1. 功能测试（3 小时）**

*用户认证模块测试：*
- 测试用例 TC-AUTH-001：正常注册流程——输入用户名 + 密码 + 确认密码 + 验证码，点击注册，预期自动登录并跳转至聊天首页。实测通过，注册后成功获取 JWT token
- 测试用例 TC-AUTH-002：重复用户名注册——使用已注册用户名再次注册，预期返回"用户名已存在"错误提示。实测通过，后端返回 USERNAME_ALREADY_REGISTER
- 测试用例 TC-AUTH-003：验证码错误——输入错误验证码，预期提示"验证码错误"。实测通过，后端读取 Redis 比对失败后抛出异常
- 测试用例 TC-AUTH-004：验证码过期——等待 5 分钟后使用旧验证码注册，预期提示"验证码已过期，请刷新后重试"。实测通过，Redis TTL 到期自动删除 key
- 测试用例 TC-AUTH-005：正常登录——输入正确用户名 + 密码 + 验证码，预期成功登录并存储 accessToken/refreshToken。实测通过
- 测试用例 TC-AUTH-006：错误密码登录——输入正确用户名 + 错误密码，预期提示"密码错误"。实测通过
- 测试用例 TC-AUTH-007：自动登录——登录时勾选"自动登录"，关闭浏览器后重新打开页面，预期自动刷新 token 并进入聊天页。实测通过
- 测试用例 TC-AUTH-008：Token 过期刷新——等待 accessToken 过期后发起请求，预期自动使用 refreshToken 续期。实测通过

*消息收发模块测试：*
- 测试用例 TC-MSG-001：私聊文字消息——用户 A 向用户 B 发送文字消息，预期双方均能实时看到消息（WebSocket 推送）。实测通过，延迟 < 200ms
- 测试用例 TC-MSG-002：私聊图片消息——用户 A 向用户 B 发送图片，预期图片上传 MinIO 成功，双方均能加载缩略图与点击查看原图。实测通过
- 测试用例 TC-MSG-003：群聊消息——用户在群聊中发送消息，预期所有在线群成员均能收到推送。实测通过
- 测试用例 TC-MSG-004：消息撤回——发送者在 2 分钟内撤回消息，预期双方均显示"消息已撤回"。实测通过
- 测试用例 TC-MSG-005：消息已读状态——用户 B 打开与用户 A 的会话，预期用户 A 侧显示"已读"。实测通过
- 测试用例 TC-MSG-006：文件消息——发送文件（pdf/txt），预期接收方可下载。实测通过

*好友与群组模块测试：*
- 测试用例 TC-FRD-001：添加好友——搜索用户名并发送好友申请，对方同意后出现在好友列表。实测通过
- 测试用例 TC-FRD-002：删除好友——右键好友选择删除，预期好友从列表消失且聊天记录保留。实测通过
- 测试用例 TC-FRD-003：好友免打扰——对好友开启免打扰后发送消息，预期不再显示未读红点。实测通过
- 测试用例 TC-GRP-001：创建群聊——填写群名称、邀请好友，预期群聊创建成功。实测通过
- 测试用例 TC-GRP-002：群聊成员管理——群主踢出成员、修改群公告。实测通过

*UI 兼容性测试：*
- 测试用例 TC-UI-001：登录页 Chrome 浏览器渲染——预期暗色背景 + 右面板 + 粒子动画正常。实测通过，存在一个问题：ChatItem 未读徽标替换后列表项布局错位
- 测试用例 TC-UI-002：注册页验证码刷新——点击验证码图片预期刷新为新验证码。实测通过
- 测试用例 TC-UI-003：1728×1117 分辨率（Surface Laptop）——预期所有页面正常缩放。实测通过
- 测试用例 TC-UI-004：暗色主题一致性——切换聊天/好友/群聊标签页，预期搜索框、列表项、在线状态风格统一。实测通过

**2. Bug 修复（1.5 小时）**
- Bug #1（P1）：ChatItem 列表项布局错位。根因：替换未读徽标 CSS 时，`display:flex` → `display:inline-block` 的全局替换误命中 `.chat-item`、`.chat-left`、`.chat-right`、`.chat-name` 等多个父级选择器。修复方法：`git checkout` 恢复原始文件，使用仅匹配 `.unread-badge` 的精准选择器重新替换，修复后布局恢复正常
- Bug #2（P2）：Safari 浏览器毛玻璃效果（backdrop-filter）不生效。根因：缺少 `-webkit-backdrop-filter` 前缀。修复方法：在 Login.vue 与 Register.vue 的面板样式中添加 webkit 前缀
- Bug #3（P3）：验证码图片首次加载时 src 为空导致控制台报错。根因：`mounted()` 中 `getCaptcha()` 是异步的，`captchaImage` 初始为空字符串。修复方法：`getCaptcha()` 开头先将 `captchaImage` 置空字符串，`<img>` 添加 `v-show="captchaImage"` 条件渲染

**3. 回归测试（0.5 小时）**
- Bug #1 修复后重新验证消息列表渲染、未读徽标数字显示（1/99/99+）
- Bug #2 修复后在 Safari 中验证登录/注册页面板毛玻璃效果
- 确认所有 3 个 Bug 修复后无新问题引入

**4. 文档编写（1.5 小时）**
- 编写《李俊毅个人开发日志》（本文档）：涵盖需求分析、架构设计、数据库设计、后端开发、消息系统、WebRTC 信令、验证码系统、前端 UI 升级、功能测试全流程，按日记录工作计划、工作内容、问题解决、AI 使用记录
- 编写《前端 UI 改造计划书》（docs/plan/PLAN.md）：将已完成改造项标记为完成状态，列出待完成项（主题变量系统重构、图标统一、响应式验证），更新影响范围与风险评估
- 将《系统设计文档》与《需求分析文档》从项目根目录整理至 `docs/课设文档/` 归档
- 将日志 MD 文件转换为标准 DOCX 格式（微软雅黑正文，A4 排版，表格带边框）

**5. 代码提交与仓库整理（0.5 小时）**
- 回顾全部改动，按功能模块拆分为独立 commit（共 9 个），遵循"一个 commit 只做一件事"原则
- 确认 .gitignore 生效，target/、node_modules/、*.iml 不入库
- 确认所有敏感信息已移除（数据库密码、JWT secret、MinIO 密钥均使用环境变量占位）
- 提交并推送至 GitHub

### 测试结果汇总

| 模块 | 用例数 | 通过 | 失败 | 通过率 |
|------|--------|------|------|--------|
| 用户认证 | 8 | 8 | 0 | 100% |
| 消息收发 | 6 | 6 | 0 | 100% |
| 好友群组 | 5 | 5 | 0 | 100% |
| UI 兼容性 | 4 | 4 | 0 | 100% |
| **合计** | **23** | **23** | **0** | **100%** |

### Bug 清单

| 编号 | 优先级 | 描述 | 状态 |
|------|--------|------|------|
| #1 | P1 | ChatItem 未读徽标修改导致列表布局错位 | ✅ 已修复 |
| #2 | P2 | Safari 毛玻璃效果不生效 | ✅ 已修复 |
| #3 | P3 | 验证码图片初始加载控制台报错 | ✅ 已修复 |

### 遇到的问题及解决方法
| 问题 | 原因 | 解决方法 |
|------|------|----------|
| ChatItem 列表项高度异常、文字不对齐 | CSS 替换 display:flex→inline-block 误扩散到父级选择器 | git checkout 恢复后精准替换 |
| Safari 面板为纯灰色无模糊 | -webkit-backdrop-filter 前缀缺失 | 添加 webkit 前缀 |
| 验证码图片首次加载报错 | captchaImage 初始为空图片 src 无效 | v-show 条件渲染 |

### AI 使用记录
- Claude：辅助列出功能测试用例清单（23 个用例覆盖认证/消息/好友/群组/UI 五大模块），本人执行全部测试并记录结果
- Claude：协助定位 Bug #1 的 CSS 选择器扩散范围（通过 git diff 逐行审查），提供精准修复方案
- Claude：辅助生成开发日志框架，具体技术细节与测试数据由本人填充
- Claude：辅助将 MD 日志转换为标准 DOCX 格式（通过 Python 脚本生成 WordprocessingML）
- 最终文档内容经本人通读修改，所有测试结果与 Bug 修复均经本人亲手验证

---

## 2026.06.25

---

## 2026.06.25（答辩准备与项目收尾）

### 当日工作计划
- 准备毕业设计答辩 PPT 与演示材料
- 整理项目演示流程脚本
- 准备答辩可能被问到的问题与回答要点
- 录制关键功能演示录屏（备选）

### 当日工作内容

**1. 答辩 PPT 制作（2.5 小时）**
- 封面页：课题名称"TYUT CHAT 校园即时通讯平台"、姓名学号、指导教师
- 课题背景页：校园即时通讯需求分析，现有方案（微信/QQ）在校园场景的不足（无校园认证体系、群组管理不便、数据不可控）
- 系统架构页：四模块后端 + Vue2 前端的整体架构图，标注技术栈（Spring Boot 3.3、Netty、Redis、MinIO、Vue 2.7）
- 核心技术亮点页（4-5 页）：
  - WebSocket 长连接 + Redis List 跨节点消息路由机制（附流程图）
  - JWT 双 Token 无状态认证 + 自动续期方案
  - Hutool 图形验证码 + Redis 一次性校验防刷机制
  - MinIO 自建对象存储 + 图片自动缩略图方案
  - Three.js 3D 粒子背景 + macOS Vibrancy 暗色 UI 设计系统
- 功能演示页（3-4 页）：登录注册、私聊消息、群聊消息、图片/文件发送、好友管理、群组管理
- 测试结果页：23 个测试用例 100% 通过率，3 个 Bug 全部修复
- 总结与展望页：已实现功能总结、后续优化方向（移动端适配、消息端到端加密、自建 TURN 服务器、AI 智能回复）
- PPT 风格与系统 UI 保持一致：暗色背景（#0a0a0c）、白色无衬线字体、macOS 风格表格与图表

**2. 演示流程脚本编写（1 小时）**
- 编写 5 分钟现场演示脚本，涵盖以下环节：
  1. 打开登录页，展示暗色玻璃面板 + 3D 粒子背景 + 验证码（30 秒）
  2. 使用预先注册的测试账号登录（15 秒）
  3. 展示聊天主界面：左侧会话列表 + 右侧消息气泡（30 秒）
  4. 发送文字消息 + 图片消息，展示实时推送（1 分钟）
  5. 创建群聊，发送群消息，展示 @提醒功能（1 分钟）
  6. 展示好友列表、在线状态、未读徽标等细节（45 秒）
  7. 展示暗色主题全页面统一风格（30 秒）
  8. 切换至 Swagger API 文档页，展示后端接口（30 秒）
- 准备两个测试账号（user_a / user_b），提前添加好友关系，确保演示流程顺畅
- 准备一张测试图片（JPG < 5MB），用于演示图片发送功能

**3. 答辩问答准备（1 小时）**
- 预估答辩教师可能提出的问题并准备回答要点：
  - Q1："为什么选择 Netty 而非直接用 Spring WebSocket？" → 答：Netty 的 NIO 模型在高并发长连接场景下性能更优（单机可支撑万级连接），事件驱动架构更灵活，支持自定义协议编解码
  - Q2："消息推送如何保证多节点部署时不丢失？" → 答：基于 Redis List 实现跨节点消息路由，每台 chat-server 独立消费自己的队列；消息持久化到 MySQL，离线用户上线后拉取历史
  - Q3："JWT Token 被窃取怎么办？" → 答：accessToken 有效期设置较短（15 分钟），refreshToken 配合自动续期；生产环境建议 HTTPS + HttpOnly Cookie 存储；后续可引入 Token 黑名单机制
  - Q4："验证码为什么选 Hutool 而不是第三方短信服务？" → 答：对于校园内网环境，图形验证码无需外部依赖、零成本、部署简单；Hutool 是项目中已使用的工具库，不引入新依赖
  - Q5："MinIO 与直接用本地文件系统有什么区别？" → 答：MinIO 支持分布式部署，多节点共享文件；提供 S3 兼容 API，后续可无缝迁移到云 OSS；自带 Web 控制台方便管理
  - Q6："前端 UI 设计的灵感来源？" → 答：参考 macOS Messages 应用的设计语言，采用暗色层级系统、1px 半透明边框、系统级毛玻璃效果；核心理念是克制与内容优先
  - Q7："项目中遇到的最大技术难点是什么？" → 答：WebSocket 多节点推送的跨节点路由问题，最终通过 Redis List 队列 + 用户-服务器映射表解决
  - Q8："安全性方面做了哪些考虑？" → 答：BCrypt 密码加密存储、JWT 签名验证、XSS 过滤拦截器、敏感词过滤、验证码防刷、SQL 参数化查询防注入

**4. 演示环境确认（0.5 小时）**
- 确保 Docker Redis 与 MinIO 容器正常运行
- 确保 MySQL 数据库服务正常
- 确保 chat-server（8878）与 chat-platform（8088）可同时启动
- 确保前端 npm run serve（8080）正常编译
- 准备备用方案：若网络异常，使用 localhost 环境演示

### 遇到的问题及解决方法
- 无技术问题。PPT 内容较多，需控制在 10-12 页以内保证演示时间。

### AI 使用记录
- Claude：辅助整理 8 个常见答辩问题的回答思路，提供技术原理的简明表述方式
- Claude：辅助优化演示脚本的时间分配（5 分钟 8 个环节），确保重点突出
- PPT 具体设计与制作由本人独立完成

---

## 技术总结

### 项目架构

```
tyut-chat/
├── chat-common/          # 公共包：枚举、工具类
├── chat-client/           # 推送 SDK：Redis 队列写入
├── chat-server/           # WebSocket 推送服务（Netty，端口 8878）
├── chat-platform/         # 业务平台服务（Spring Boot，端口 8088）
│   ├── controller/        # REST 控制器（Login、File、Friend、Group、Message、Captcha 等）
│   ├── service/           # 业务逻辑层
│   ├── mapper/            # MyBatis-Plus 数据访问层
│   ├── entity/            # 数据库实体
│   ├── dto/               # 数据传输对象
│   ├── vo/                # 视图对象
│   ├── config/            # Spring 配置（Redis、MinIO、JWT、MVC、Swagger）
│   ├── interceptor/       # 认证拦截器、XSS 拦截器
│   └── thirdparty/        # 第三方服务封装（MinIO）
└── chat-web/              # Vue 2 前端
    ├── src/view/          # 页面组件（Login、Register、Chat、Friend、Group、Home）
    ├── src/components/    # 通用组件（HeadImage、ChatItem、ChatMessageItem、FriendItem 等）
    ├── src/api/           # API 层（axios 封装、WebSocket 客户端、auth 工具）
    ├── src/store/         # Pinia 状态管理
    └── src/assets/        # 静态资源（iconfont、emoji、style）
```

### 核心技术亮点
1. **跨节点消息推送**：基于 Redis List 实现多 chat-server 实例的消息路由，每个节点独立消费自己的队列
2. **无状态认证**：JWT 双 token 机制（accessToken 短期 + refreshToken 长期），支持自动续期
3. **图形验证码**：Hutool 线段干扰 + Redis 5 分钟过期 + 一次性使用
4. **对象存储**：MinIO 自建 OSS，bucket 公开读策略，图片自动缩略图
5. **3D 粒子背景**：Three.js 原生 WebGL，多面体浮动 + 粒子环 + Shader 极光丝带，性能优化（requestAnimationFrame + lerp 平滑）
6. **macOS Vibrancy 设计系统**：暗色层级（#0a0a0c → #1c1c1e → #2c2c2e → #3a3a3c）、系统级 backdrop-blur、1px 半透明边框、无渐变无大阴影

### 统计数据
- 后端 Java 文件：约 70 个（entity/dto/vo/controller/service/mapper/config/thirdparty）
- 前端 Vue 组件：约 20 个
- 数据库表：8 张
- REST API 端点：约 30 个
- WebSocket 消息类型：20+ 种
- Git 提交：9 个（2026.06.23 — 06.24）
- 代码总行数：约 15,000 行（含注释与空行）

---

> 📸 **待补充截图清单**（请准备后告知，我将插入文档对应位置）：
> 1. 项目整体目录结构截图（IDEA 项目树）
> 2. 登录页面完整截图（暗色背景 + 右侧玻璃面板 + TYUT CHAT 品牌区 + 验证码）
> 3. 注册页面完整截图
> 4. 聊天主界面截图（左侧会话列表 + 右侧 iMessage 风格气泡）
> 5. 未读消息红色圆点徽标特写（含 99+ 显示）
> 6. 在线状态绿色小点特写（#30d158）
> 7. 好友列表页面截图
> 8. 群聊详情页面截图
> 9. MinIO 控制台截图（bucket 与文件列表）
> 10. Redis 客户端截图（captcha key 示例）
> 11. 数据库 ER 图或 Navicat 表结构截图
> 12. API 文档页面截图（Knife4j / Swagger）
